<Window x:Class="RestAll.Desktop.App.Views.TablesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RestAll - Tables" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{DynamicResource TablesTitle}" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"/>

        <Button Grid.Row="1" 
                Content="{DynamicResource LoadTablesButton}" 
                Height="35" 
                Width="150" 
                HorizontalAlignment="Left"
                Command="{Binding LoadTablesCommand}"
                Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" 
                   Text="{Binding StatusMessage}" 
                   HorizontalAlignment="Right"
                   VerticalAlignment="Center"
                   Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="2">
            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" 
                                BorderThickness="1" 
                                Padding="15" 
                                Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                           Text="{Binding Number}" 
                                           FontSize="24" 
                                           FontWeight="Bold"
                                           Margin="0,0,20,0"
                                           VerticalAlignment="Center"/>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Capacity, StringFormat=Capacity: {0}}" 
                                               FontSize="14"
                                               Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Status, StringFormat=Status: {0}}" 
                                               FontSize="14"/>
                                </StackPanel>

                                <ComboBox Grid.Column="2" 
                                          Width="150"
                                          VerticalAlignment="Center"
                                          Loaded="StatusComboBox_Loaded"
                                          DropDownClosed="StatusComboBox_DropDownClosed">
                                    <ComboBoxItem Content="{DynamicResource AvailableStatus}" Tag="0"/>
                                    <ComboBoxItem Content="{DynamicResource OccupiedStatus}" Tag="1"/>
                                    <ComboBoxItem Content="{DynamicResource ReservedStatus}" Tag="2"/>
                                    <ComboBoxItem Content="{DynamicResource CleaningStatus}" Tag="3"/>
                                </ComboBox>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
